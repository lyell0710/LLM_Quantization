# manifest: EXP-002 运行日志(provenance 同各自 JSON 首字段)
awq_gptq_run.log <- 对应同名 json 的 .provenance 字段
awq_run.log <- 对应同名 json 的 .provenance 字段

# 勘注 2026-08-24(审计收尾批次)
# - 固定命名文件({awq,awq_gptq}_g128.json 与 *_run.log)产生于 UTC 前缀命名
#   规范生效前,按 grandfather 保留原名;run_all2.sh 现已改为 UTC 前缀新文件 +
#   同名拒覆盖 + worktree dirty 拒跑。
