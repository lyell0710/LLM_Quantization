# 怎么读这个项目(LLM_Quantization · 三种量化方法从零实现)

> **上游**:跨仓的先后次序见 `Resume/STUDY_MAP.md`;本文只讲**进了这个仓之后怎么读**。
> **同级**:各仓根目录的同名文件结构一致,读熟一个就会读其余五个。
> **与本仓已有学习文档的分工**:`docs/HOW_TO_LEARN_A_QUANT_METHOD.md` 是
> **怎么学一个新的量化方法**(可复用 playbook,六步),本文是**怎么读已经建成的这个仓**。
> **下游**:量化的**部署形态**在 `llm-engine`(W8A8 接引擎)与 `Kernel_Optimazation/w8a8`
> (kernel 链路)——本仓做的是算法侧,那两个仓做的是落地侧,读完这里应该接着去那边。

---

## 总纲:这个仓的价值是"同协议对照",不是三份实现

GPTQ、AWQ、SmoothQuant 三份从零实现,单看每一份都不稀奇 ——
网上有的是。本仓真正的产出是:**三者在同一个协议下被量到同一张表上**。

同协议意味着:同模型(Qwen2.5-0.5B)、同评测窗(2048/1536)、同计分 token 数
(298302)、同确定性 greedy scoring。**只有这样,"GPTQ 收回 61.6% 而 AWQ 收回 31.9%"
才是一句有意义的话。**

所以读这个仓,最先要读的不是任何一份算法实现,是**协议本身**
(`README.md` 主要结果表上面那段测量协议说明 + `docs/lectures/01` 的对照设计)。
协议没读懂,后面所有数字都只是数字。

第二条:**本仓的 `src/` 只有 526 行**(gptq 197 + awq 149 + smoothquant 103 + quant_linear 77)。
量化算法的代码量从来不大,难的是"怎么证明它真的有效"。

---

## 零、第一次进仓:按顺序打开(80 分钟)

| 步 | 打开什么 | 只看什么 | 时间 |
|---|---|---|---|
| 1 | `README.md` | 主要结果表 **+ 表上方那段测量协议说明**(那段比表重要) | 15 min |
| 2 | `docs/theory/01_gptq.md` | 全文(五节:结论/机制/本项目实证/追问 Q&A/延伸) | 15 min |
| 3 | `src/gptq.py` | **只读误差补偿那一段**,不看数据搬运 | 20 min |
| 4 | `records/EXP-001_gptq_from_scratch.md` | §1 假设 + §5 结果 + **§7 异常** | 15 min |
| 5 | 白板自测 | 不看代码,写出 GPTQ 的逐列量化 + 误差补偿更新式,说清它比 RTN 多做了什么 | 15 min |

第 3 步的定位:GPTQ 全文 197 行,但真正的算法只有误差补偿那十几行。
**其余是 Hessian 估计、分块、打包 —— 第一遍全部跳过。**

配套白板卡:`docs/talk/whiteboard_card_gptq_update.md`(一页纸的更新式推导)。

---

## 一、完整阅读顺序(精确到文件)

三种方法**按难度而非按仓内编号**排。每一行走完下面的「固定四步」。

| # | 方法 | 入口 | src | records | 走完能答的一个问题 |
|---|---|---|---|---|---|
| 1 | GPTQ(二阶) | `docs/theory/01_gptq.md` | `src/gptq.py` | EXP-001 | 误差补偿凭什么比 RTN 多收回一倍 |
| 2 | AWQ(一阶) | `docs/theory/02_awq.md` | `src/awq.py` | EXP-002 | 为什么"保护强度按层搜"而不是全局常数 |
| 3 | 叠加 | — | `scripts/run_w4a16.py` | EXP-002 | 两个方向正交,为什么在 0.5B 上只 +1.0pp |
| 4 | SmoothQuant(W8A8) | `docs/theory/03_smoothquant.md` | `src/smoothquant.py` | EXP-003 | 为什么 α 是权衡旋钮而不是"加了就好" |
| 5 | real quant 闭环 | — | `src/quant_linear.py` | EXP-001 §5 | fake quant 与真打包之间差什么 |
| — | 部署形态 | — | → `llm-engine/src/smoothquant.py` | llm-engine EXP-D24 | 迁移因子为什么必须折进前置 norm |

### 每份实现的固定四步(30-50 分钟)

| 步 | 动作 |
|---|---|
| 1 | 读 `docs/theory/NN_*.md` 的**前两节**(一句话结论 + 机制) |
| 2 | 读 `src/*.py` 的**模块 docstring** —— 本仓的 docstring 写了算法一句话、接口契约、性能特征(带实测锚) |
| 3 | 只读算法核心那十几行,跳过数据搬运与打包 |
| 4 | 读对应 `records/EXP-00N` 的 §6(分析)与 **§7(异常与开放问题)** |

### 两个里程碑

- 走完 **#3**:能讲清一阶方法与二阶方法的分野,以及"叠加正交但重叠"这个反直觉结论。
- 走完 **#6(部署形态)**:能完整回答"量化怎么落到推理里",而不是停在离线 PPL。

**第二个里程碑跨仓**,而这恰恰是本仓最容易断的地方 ——
只做到 PPL 表就停下,面试时会被"那你部署过吗"一句问住。

---

## 二、文档分层:按目的进

| 你的目的 | 进哪一层 | 时间 |
|---|---|---|
| 知道有什么、结论是什么 | `README.md` 主要结果表 + 关键发现 | 15 min |
| 学**怎么学一个新方法** | `docs/HOW_TO_LEARN_A_QUANT_METHOD.md`(六步 playbook) | 10 min |
| 某个方法的一页纸速查 | `docs/theory/01`~`03`(五节格式) | 15 min/篇 |
| 三法的对照设计与推导 | `docs/lectures/01_three_quant_methods.md` 的**对应小节** | 30-60 min |
| 查数字怎么来的、可不可信 | `records/EXP-00N` 的 §2 / §5 / **§7** | 10 min |
| 练面试怎么讲 | `docs/talk/quant_walkthrough.md`(30 分钟五段) + 白板卡 | 20 min |

**本仓特有的两条纪律**:

1. **PPL 是自定义协议,只能臂间相对比较,不能与文献绝对值对比。**
   README 里写死了这一条。引用任何 PPL 数字时必须带这个限定 ——
   忘了它,面试时报一个"我的 PPL 是 12.76"会被追问到崩。
2. **早期 AutoAWQ 侧实践未随本仓公开**(含课程作业与 vendored 第三方源码,
   另存于私有仓)。其 PPL 属**异协议**(HF eval、不同模型),绝对值不与
   `src/` 从零实现混在一张表里 —— 该口径见 LEDGER。

---

## 三、代码怎么看

### 读法一 · 三份实现用"同构对照"读

`gptq.py` / `awq.py` / `smoothquant.py` 是**同构**的:都提供
「量化入口 + 评测钩子」,都以 fake quant 形式接进评测管线。
所以最高效的读法不是依次通读,而是**对照读同一个位置**:

| 对照点 | GPTQ | AWQ | SmoothQuant |
|---|---|---|---|
| 用什么信息决定怎么量化 | Hessian(二阶) | 激活幅度(一阶) | 激活/权重的逐通道 absmax |
| 作用对象 | 权重,逐列 | 权重,逐通道缩放 | 权重与激活之间的**难度迁移** |
| 有没有校准 | 有(Hessian) | 有(best-scale 搜索) | 有(actmax 收集) |
| 反例臂是什么 | RTN | scale≡1 | α<0 即 naive(s≡1) |

**最后一行最值得抄**:每份实现都内建了"不做这个方法"的臂,共用同一条管线。
`smoothquant.py` 的 docstring 里写得最清楚:`alpha<0` 为 naive 臂,
与 smooth 臂共用同一 INT8 格式与管线,唯一差异 = 是否迁移。
**这是控制变量法在代码层面的落地** —— 不是跑两个脚本比一比,是同一份代码里切一个开关。

### 读法二 · 先读 docstring,它写了三样别处没有的东西

本仓的模块 docstring 格式统一,含:**算法一句话 / 接口契约 / 性能特征(带实测锚)**。
例如 `src/smoothquant.py` 的 docstring 直接给出了 α 扫描的结果与反例
(α=0.25 反而更差)。**读完 docstring 再读代码,代码会变得很短。**

### 读法三 · 只读算法核心,跳过打包

量化实现里通常七成代码是数据搬运(分块、转置、INT4 两枚打一字节)。
第一遍全部跳过。等到要回答"fake quant 和真部署差什么"时,
再回来读 `src/quant_linear.py`(77 行)与 EXP-001 §5 的打包逐元素断言。

### 读法四 · 先跑再读

```bash
bash scripts/run_all.sh        # 或 run_w4a16.py / run_w8a8.py 单跑
python scripts/plot_recovery.py
```

带着"为什么叠加只多了 1.0pp"这种具体疑问去读代码。

---

## 四、自测验收

不看代码,在白纸上做:

| 里程碑 | 验收动作 |
|---|---|
| #1 后 | 写出 GPTQ 的逐列量化 + 误差补偿更新式,说清它与 RTN 的差别在哪一项 |
| #3 后 | 讲清一阶/二阶的分野;解释"方向正交但在 0.5B 上高度重叠"为什么不矛盾 |
| #4 后 | 讲清 α 的权衡:α 越大激活越轻权重越重,并说出 α=0.25 反例说明了什么 |
| 跨仓后 | 讲清迁移因子折进前置 RMSNorm 为什么是零开销,以及哪些 Linear 折不进去 |

面试前一晚:`docs/talk/quant_walkthrough.md`(30 分钟五段,自家数字带跑)+
讲义的**误区与边界**节 + README 的 PPL 协议限定。

---

## 五、当前缺口

1. **只在 0.5B 上做过。** 三法的相对强弱在更大模型上会变
   (SmoothQuant 的收益随规模增长,本仓 §关键发现 已注明这是反向证据)。
   引用结论时必须带模型尺寸。
2. **AWQ 是 per-linear 简化实现、无 clip**,与论文完整版有差距,
   README 已标注 —— 报"AWQ 收回 31.9%"时不能省掉这个限定。
3. **算法侧与部署侧断开**:本仓做到 PPL 表为止,W8A8 的部署形态在
   `llm-engine`。两边的 SmoothQuant 实现是**两份**(这边 fake-quant hook 做研究,
   那边折进 norm 做部署),读的时候不要混。
4. `docs/theory/` 缺一篇「三法对照总纲」,现在这条主线只存在于讲义里。
